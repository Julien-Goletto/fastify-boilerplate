export interface User {
  id: number;
  pseudo: string;
  is_admin?: boolean;
  password?: string;
};

// const userService = {
//   async createUser (user: User ) {
//     await
//   }
// }

// export default userService;
